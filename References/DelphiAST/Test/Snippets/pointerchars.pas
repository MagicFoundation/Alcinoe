unit pointerchars;

interface

implementation

procedure FormKeyPress(Sender: TObject; var Key: Char);
var
  P: PInteger;
  Arr: array of Integer;
begin
   if Arr[P^] > 0 then
   begin
     // some code
   end;

   if Key = ^\ then 
   begin
      //some code
   end;

   if Key = ^M then 
   begin
      //some code
   end;
end;

end.

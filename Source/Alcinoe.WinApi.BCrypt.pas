unit Alcinoe.WinApi.BCrypt;

// ***************************************************************** //
// This unit was automatically generated by CHeaderWrapperGenerator. //
// Do not edit manually.                                             //
// ***************************************************************** //

interface

{$I Alcinoe.inc}

{$WARN SYMBOL_PLATFORM OFF}
{$SCOPEDENUMS OFF}
{$MINENUMSIZE 4}
{$A+}

uses
  WinApi.Windows,
  Alcinoe.WinApi.Windows;

//
// The __BCRYPT_H__ is used by other header files to conditionally define
// BCrypt-dependent extensions when bcrypt.h has been included.
//
// #define __BCRYPT_H__

type NTSTATUS = LONG;
type PNTSTATUS = ^NTSTATUS;

// #define BCRYPT_SUCCESS(Status) (((NTSTATUS)(Status)) >= 0)

// #define CONST const

// #define IN

// #define OUT

// #define OPTIONAL

//
//  Alignment macros
//

//
// BCRYPT_OBJECT_ALIGNMENT must be a power of 2
// We align all our internal data structures to 16 to
// allow fast XMM memory accesses.
// BCrypt callers do not need to take any alignment precautions.
//
const BCRYPT_OBJECT_ALIGNMENT = 16;

//
// BCRYPT_STRUCT_ALIGNMENT is an alignment macro that we no longer use.
// It used to align declspec(align(4)) on x86 and declspec(align(8)) on x64/ia64 but
// all structures that used it contained a pointer so they were already 4/8 aligned.
//
// #define BCRYPT_STRUCT_ALIGNMENT

//
// DeriveKey KDF Types
//
const BCRYPT_KDF_HASH = 'HASH';
const BCRYPT_KDF_HMAC = 'HMAC';
const BCRYPT_KDF_TLS_PRF = 'TLS_PRF';

const BCRYPT_KDF_SP80056A_CONCAT = 'SP800_56A_CONCAT';

const BCRYPT_KDF_RAW_SECRET = 'TRUNCATE';

const BCRYPT_KDF_HKDF = 'HKDF';

//
// DeriveKey KDF BufferTypes
//
// For BCRYPT_KDF_HASH and BCRYPT_KDF_HMAC operations, there may be an arbitrary
// number of KDF_SECRET_PREPEND and KDF_SECRET_APPEND buffertypes in the
// parameter list.  The BufferTypes are processed in order of appearence
// within the parameter list.
//
const KDF_HASH_ALGORITHM = $0;
const KDF_SECRET_PREPEND = $1;
const KDF_SECRET_APPEND = $2;
const KDF_HMAC_KEY = $3;
const KDF_TLS_PRF_LABEL = $4;
const KDF_TLS_PRF_SEED = $5;
const KDF_SECRET_HANDLE = $6;

const KDF_TLS_PRF_PROTOCOL = $7;
const KDF_ALGORITHMID = $8;
const KDF_PARTYUINFO = $9;
const KDF_PARTYVINFO = $A;
const KDF_SUPPPUBINFO = $B;
const KDF_SUPPPRIVINFO = $C;

const KDF_LABEL = $D;
const KDF_CONTEXT = $E;
const KDF_SALT = $F;
const KDF_ITERATION_COUNT = $10;
//
//
// Parameters for BCrypt(/NCrypt)KeyDerivation:
// Generic parameters:
// KDF_GENERIC_PARAMETER and KDF_HASH_ALGORITHM are the generic parameters that can be passed for the following KDF algorithms:
// BCRYPT/NCRYPT_SP800108_CTR_HMAC_ALGORITHM
//      KDF_GENERIC_PARAMETER = KDF_LABEL||0x00||KDF_CONTEXT
// BCRYPT/NCRYPT_SP80056A_CONCAT_ALGORITHM
//      KDF_GENERIC_PARAMETER = KDF_ALGORITHMID || KDF_PARTYUINFO || KDF_PARTYVINFO {|| KDF_SUPPPUBINFO } {|| KDF_SUPPPRIVINFO }
// BCRYPT/NCRYPT_PBKDF2_ALGORITHM
//      KDF_GENERIC_PARAMETER = KDF_SALT
// BCRYPT/NCRYPT_CAPI_KDF_ALGORITHM
//      KDF_GENERIC_PARAMETER = Not used
// BCRYPT/NCRYPT_TLS1_1_KDF_ALGORITHM
//      KDF_GENERIC_PARAMETER = Not used
// BCRYPT/NCRYPT_TLS1_2_KDF_ALGORITHM
//      KDF_GENERIC_PARAMETER = Not used
// BCRYPT/NCRYPT_HKDF_ALGORITHM
//      KDF_GENERIC_PARAMETER = Not used
//
// KDF specific parameters:
// For BCRYPT/NCRYPT_SP800108_CTR_HMAC_ALGORITHM:
//      KDF_HASH_ALGORITHM, KDF_LABEL and KDF_CONTEXT are required
// For BCRYPT/NCRYPT_SP80056A_CONCAT_ALGORITHM:
//      KDF_HASH_ALGORITHM, KDF_ALGORITHMID, KDF_PARTYUINFO, KDF_PARTYVINFO are required
//      KDF_SUPPPUBINFO, KDF_SUPPPRIVINFO are optional
// For BCRYPT/NCRYPT_PBKDF2_ALGORITHM
//      KDF_HASH_ALGORITHM is required
//      KDF_ITERATION_COUNT, KDF_SALT are optional
//      Iteration count, (if not specified) will default to 10,000
// For BCRYPT/NCRYPT_CAPI_KDF_ALGORITHM
//      KDF_HASH_ALGORITHM is required
// For BCRYPT/NCRYPT_TLS1_1_KDF_ALGORITHM
//      KDF_TLS_PRF_LABEL is required
//      KDF_TLS_PRF_SEED is required
// For BCRYPT/NCRYPT_TLS1_2_KDF_ALGORITHM
//      KDF_HASH_ALGORITHM is required
//      KDF_TLS_PRF_LABEL is required
//      KDF_TLS_PRF_SEED is required
// For BCRYPT/NCRYPT_HKDF_ALGORITHM
//      KDF_HKDF_INFO is optional
//
const KDF_GENERIC_PARAMETER = $11;
const KDF_KEYBITLENGTH = $12;

const KDF_HKDF_SALT = $13; // This is used only for testing purposes
const KDF_HKDF_INFO = $14;

//
// DeriveKey Flags:
//
// KDF_USE_SECRET_AS_HMAC_KEY_FLAG causes the secret agreement to serve also
// as the HMAC key.  If this flag is used, the KDF_HMAC_KEY parameter should
// NOT be specified.
//
const KDF_USE_SECRET_AS_HMAC_KEY_FLAG = $1;

//
// BCrypt structs
//

type
  __BCRYPT_KEY_LENGTHS_STRUCT = record
    dwMinLength: ULONG;
    dwMaxLength: ULONG;
    dwIncrement: ULONG;
  end;
  BCRYPT_KEY_LENGTHS_STRUCT = __BCRYPT_KEY_LENGTHS_STRUCT;

type BCRYPT_AUTH_TAG_LENGTHS_STRUCT = BCRYPT_KEY_LENGTHS_STRUCT;

type
  _BCRYPT_OID = record
    cbOID: ULONG;
    pbOID: PUCHAR;
  end;
  BCRYPT_OID = _BCRYPT_OID;
  PBCRYPT_OID = ^BCRYPT_OID;

type
  _BCRYPT_OID_LIST = record
    dwOIDCount: ULONG;
    pOIDs: PBCRYPT_OID;
  end;
  BCRYPT_OID_LIST = _BCRYPT_OID_LIST;

type
  _BCRYPT_PKCS1_PADDING_INFO = record
    pszAlgId: LPCWSTR;
  end;
  BCRYPT_PKCS1_PADDING_INFO = _BCRYPT_PKCS1_PADDING_INFO;

type
  _BCRYPT_PSS_PADDING_INFO = record
    pszAlgId: LPCWSTR;
    cbSalt: ULONG;
  end;
  BCRYPT_PSS_PADDING_INFO = _BCRYPT_PSS_PADDING_INFO;

type
  _BCRYPT_OAEP_PADDING_INFO = record
    pszAlgId: LPCWSTR;
    pbLabel: PUCHAR;
    cbLabel: ULONG;
  end;
  BCRYPT_OAEP_PADDING_INFO = _BCRYPT_OAEP_PADDING_INFO;

type
  _BCRYPT_PQDSA_PADDING_INFO = record
    pbCtx: PUCHAR;
    cbCtx: ULONG;
    pszPrehashAlgId: LPCWSTR;
  end;
  BCRYPT_PQDSA_PADDING_INFO = _BCRYPT_PQDSA_PADDING_INFO;

const BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO_VERSION = 1;

const BCRYPT_AUTH_MODE_CHAIN_CALLS_FLAG = $00000001;
const BCRYPT_AUTH_MODE_IN_PROGRESS_FLAG = $00000002;

type
  _BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO = record
    cbSize: ULONG;
    dwInfoVersion: ULONG;
    pbNonce: PUCHAR;
    cbNonce: ULONG;
    pbAuthData: PUCHAR;
    cbAuthData: ULONG;
    pbTag: PUCHAR;
    cbTag: ULONG;
    pbMacContext: PUCHAR;
    cbMacContext: ULONG;
    cbAAD: ULONG;
    cbData: ULONGLONG;
    dwFlags: ULONG;
  end;
  BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO = _BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO;
  PBCRYPT_AUTHENTICATED_CIPHER_MODE_INFO = ^_BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO;

// #define BCRYPT_INIT_AUTH_MODE_INFO(_AUTH_INFO_STRUCT_) RtlZeroMemory((&_AUTH_INFO_STRUCT_), sizeof(BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO)); (_AUTH_INFO_STRUCT_).cbSize = sizeof(BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO); (_AUTH_INFO_STRUCT_).dwInfoVersion = BCRYPT_AUTHENTICATED_CIPHER_MODE_INFO_VERSION;

//
// BCrypt String Properties
//

// BCrypt(Import/Export)Key BLOB types
const BCRYPT_OPAQUE_KEY_BLOB = 'OpaqueKeyBlob';
const BCRYPT_KEY_DATA_BLOB = 'KeyDataBlob';

const BCRYPT_AES_WRAP_KEY_BLOB = 'Rfc3565KeyWrapBlob';

const BCRYPT_PKCS11_RSA_AES_WRAP_KEY_BLOB = 'PKCS11RsaAesWrapBlob';

const BCRYPT_PKCS11_RSA_AES_WRAP_BLOB_MAGIC = $57504152; // 'RAPW' for RSA-AES-PAD-WRAP (PKCS11-RSA-AES-WRAP)
type
  _BCRYPT_PKCS11_RSA_AES_WRAP_BLOB = record
    dwMagic: ULONG; // BCRYPT_PKCS11_RSA_AES_WRAP_BLOB_MAGIC
    cbKey: ULONG; // Number of bytes in the binary PKCS#11 wrapped key blob
    cbPaddingAlgId: ULONG; // Number of bytes in OAEP Padding algorithm per OAEPParams in PKCS#11 specification
    cbPaddingLabel: ULONG; // Number of bytes in OAEP Padding label per OAEPParams in PKCS#11 specification
    // UCHAR Key[cbKey]; -- PKCS#11 binary blob
    // UCHAR PaddingAlgId[cbPaddingAlgId]; -- OAEP Padding information for PKCS#11 unwrapping
    // UCHAR PaddingLabel[cbPaddingLabel]; -- OAEP Padding information for PKCS#11 unwrapping
  end;
  BCRYPT_PKCS11_RSA_AES_WRAP_BLOB = _BCRYPT_PKCS11_RSA_AES_WRAP_BLOB;
  PBCRYPT_PKCS11_RSA_AES_WRAP_BLOB = ^_BCRYPT_PKCS11_RSA_AES_WRAP_BLOB;

// BCryptGetProperty strings
const BCRYPT_OBJECT_LENGTH = 'ObjectLength';
const BCRYPT_ALGORITHM_NAME = 'AlgorithmName';
const BCRYPT_PROVIDER_HANDLE = 'ProviderHandle';
const BCRYPT_CHAINING_MODE = 'ChainingMode';
const BCRYPT_BLOCK_LENGTH = 'BlockLength';
const BCRYPT_KEY_LENGTH = 'KeyLength';
const BCRYPT_KEY_OBJECT_LENGTH = 'KeyObjectLength';
const BCRYPT_KEY_STRENGTH = 'KeyStrength';
const BCRYPT_KEY_LENGTHS = 'KeyLengths';
const BCRYPT_BLOCK_SIZE_LIST = 'BlockSizeList';
const BCRYPT_EFFECTIVE_KEY_LENGTH = 'EffectiveKeyLength';
const BCRYPT_HASH_LENGTH = 'HashDigestLength';
const BCRYPT_HASH_OID_LIST = 'HashOIDList';
const BCRYPT_PADDING_SCHEMES = 'PaddingSchemes';
const BCRYPT_SIGNATURE_LENGTH = 'SignatureLength';
const BCRYPT_HASH_BLOCK_LENGTH = 'HashBlockLength';
const BCRYPT_AUTH_TAG_LENGTH = 'AuthTagLength';

const BCRYPT_FUNCTION_NAME_STRING = 'FunctionNameString';
const BCRYPT_CUSTOMIZATION_STRING = 'CustomizationString';

const BCRYPT_PRIMITIVE_TYPE = 'PrimitiveType';
const BCRYPT_IS_KEYED_HASH = 'IsKeyedHash';

const BCRYPT_IS_REUSABLE_HASH = 'IsReusableHash';
const BCRYPT_MESSAGE_BLOCK_LENGTH = 'MessageBlockLength';

const BCRYPT_PUBLIC_KEY_LENGTH = 'PublicKeyLength';

// Additional BCryptGetProperty strings for the RNG Platform Crypto Provider
const BCRYPT_PCP_PLATFORM_TYPE_PROPERTY = 'PCP_PLATFORM_TYPE';
const BCRYPT_PCP_PROVIDER_VERSION_PROPERTY = 'PCP_PROVIDER_VERSION';

const BCRYPT_MULTI_OBJECT_LENGTH = 'MultiObjectLength';

const BCRYPT_IS_IFX_TPM_WEAK_KEY = 'IsIfxTpmWeakKey';

//
// Additional properties for the HKDF on BCRYPT_KEY_HANDLE (and
// BCRYPT_SECRET_HANDLE). Both the hash algorithm property and
// one of the "Finalize" properties are required for the key
// (or secret) to be usable.
//
// When the available inputs are the input keying material (IKM)
// and the salt then the "SALT_AND_FINALIZE" path should be used:
//  - First the function which creates the key (or secret) takes
//  as input the IKM.
//  - Then the hash algorithm should be set via the BCRYPT_HKDF_HASH_ALGORITHM
//  property on BCryptSetProperty.
//  - Finally the salt is input via the BCRYPT_HKDF_SALT_AND_FINALIZE
//  property. The salt parameter is optional; thus the property input
//  is allowed to be NULL.
//
// When the available input is the pseudorandom key (PRK) then
// the "PRK_AND_FINALIZE" path should be used:
//  - First the function which creates the key (or secret) takes
//  as input the PRK.
//  - Then the hash algorithm should be set via the BCRYPT_HKDF_HASH_ALGORITHM
//  property on BCryptSetProperty.
//  - Finally the key (or secret) is finalized via the
//  BCRYPT_HKDF_PRK_AND_FINALIZE property. In this case the input property
//  must be NULL since the PRK was already passed in.
//
// After setting one of the two "Finalize" properties the key
// (or the secret) is finalized and can be used to derive the
// HKDF output.
//
const BCRYPT_HKDF_HASH_ALGORITHM = 'HkdfHashAlgorithm';
const BCRYPT_HKDF_SALT_AND_FINALIZE = 'HkdfSaltAndFinalize';
const BCRYPT_HKDF_PRK_AND_FINALIZE = 'HkdfPrkAndFinalize';

const BCRYPT_KEM_SHARED_SECRET_LENGTH = 'KEMSharedSecretLength';
const BCRYPT_KEM_CIPHERTEXT_LENGTH = 'KEMCiphertextLength';
const BCRYPT_PARAMETER_SET_NAME = 'ParameterSetName';

// BCryptSetProperty strings
const BCRYPT_INITIALIZATION_VECTOR = 'IV';

// Property Strings
const BCRYPT_CHAIN_MODE_NA = 'ChainingModeN/A';
const BCRYPT_CHAIN_MODE_CBC = 'ChainingModeCBC';
const BCRYPT_CHAIN_MODE_ECB = 'ChainingModeECB';
const BCRYPT_CHAIN_MODE_CFB = 'ChainingModeCFB';
const BCRYPT_CHAIN_MODE_CCM = 'ChainingModeCCM';
const BCRYPT_CHAIN_MODE_GCM = 'ChainingModeGCM';

// Supported RSA Padding Types
const BCRYPT_SUPPORTED_PAD_ROUTER = $00000001;
const BCRYPT_SUPPORTED_PAD_PKCS1_ENC = $00000002;
const BCRYPT_SUPPORTED_PAD_PKCS1_SIG = $00000004;
const BCRYPT_SUPPORTED_PAD_OAEP = $00000008;
const BCRYPT_SUPPORTED_PAD_PSS = $00000010;

//
//      BCrypt Flags
//

const BCRYPT_PROV_DISPATCH = $00000001; // BCryptOpenAlgorithmProvider

const BCRYPT_BLOCK_PADDING = $00000001; // BCryptEncrypt/Decrypt

const BCRYPT_GENERATE_IV = $00000020; // BCryptGenerateSymmetricKey BCryptEncrypt

// RSA padding schemes
const BCRYPT_PAD_NONE = $00000001;
const BCRYPT_PAD_PKCS1 = $00000002; // BCryptEncrypt/Decrypt BCryptSignHash/VerifySignature
const BCRYPT_PAD_OAEP = $00000004; // BCryptEncrypt/Decrypt
const BCRYPT_PAD_PSS = $00000008; // BCryptSignHash/VerifySignature
const BCRYPT_PAD_PKCS1_OPTIONAL_HASH_OID = $00000010; // BCryptVerifySignature

const BCRYPT_PAD_PQDSA = $00000020; // BCryptSignHash/VerifySignature
const BCRYPT_MLDSA_EXTERNAL_MU = $00000040; // BCryptSignHash/VerifySignature

const BCRYPTBUFFER_VERSION = 0;

type
  _BCryptBuffer = record
    cbBuffer: ULONG; // Length of buffer, in bytes
    BufferType: ULONG; // Buffer type
    pvBuffer: PVOID; // Pointer to buffer
  end;
  BCryptBuffer = _BCryptBuffer;
  PBCryptBuffer = ^_BCryptBuffer;

type
  _BCryptBufferDesc = record
    ulVersion: ULONG; // Version number
    cBuffers: ULONG; // Number of buffers
    pBuffers: PBCryptBuffer; // Pointer to array of buffers
  end;
  BCryptBufferDesc = _BCryptBufferDesc;
  PBCryptBufferDesc = ^_BCryptBufferDesc;

//
// Primitive handles
//

type BCRYPT_HANDLE = PVOID;
type BCRYPT_ALG_HANDLE = PVOID;
type PBCRYPT_ALG_HANDLE = ^BCRYPT_ALG_HANDLE;
type BCRYPT_KEY_HANDLE = PVOID;
type PBCRYPT_KEY_HANDLE = ^BCRYPT_KEY_HANDLE;
type BCRYPT_HASH_HANDLE = PVOID;
type PBCRYPT_HASH_HANDLE = ^BCRYPT_HASH_HANDLE;
type BCRYPT_SECRET_HANDLE = PVOID;
type PBCRYPT_SECRET_HANDLE = ^BCRYPT_SECRET_HANDLE;

//
// Structures used to represent key blobs.
//

const BCRYPT_PUBLIC_KEY_BLOB = 'PUBLICBLOB';
const BCRYPT_PRIVATE_KEY_BLOB = 'PRIVATEBLOB';

type
  _BCRYPT_KEY_BLOB = record
    Magic: ULONG;
  end;
  BCRYPT_KEY_BLOB = _BCRYPT_KEY_BLOB;

// The BCRYPT_RSAPUBLIC_BLOB and BCRYPT_RSAPRIVATE_BLOB blob types are used
// to transport plaintext RSA keys. These blob types will be supported by
// all RSA primitive providers.
// The BCRYPT_RSAPRIVATE_BLOB includes the following values:
// Public Exponent
// Modulus
// Prime1
// Prime2

const BCRYPT_RSAPUBLIC_BLOB = 'RSAPUBLICBLOB';
const BCRYPT_RSAPRIVATE_BLOB = 'RSAPRIVATEBLOB';
const LEGACY_RSAPUBLIC_BLOB = 'CAPIPUBLICBLOB';
const LEGACY_RSAPRIVATE_BLOB = 'CAPIPRIVATEBLOB';

const BCRYPT_RSAPUBLIC_MAGIC = $31415352; // RSA1
const BCRYPT_RSAPRIVATE_MAGIC = $32415352; // RSA2

type
  _BCRYPT_RSAKEY_BLOB = record
    Magic: ULONG;
    BitLength: ULONG;
    cbPublicExp: ULONG;
    cbModulus: ULONG;
    cbPrime1: ULONG;
    cbPrime2: ULONG;
  end;
  BCRYPT_RSAKEY_BLOB = _BCRYPT_RSAKEY_BLOB;

// The BCRYPT_RSAFULLPRIVATE_BLOB blob type is used to transport
// plaintext private RSA keys.  It includes the following values:
// Public Exponent
// Modulus
// Prime1
// Prime2
// Private Exponent mod (Prime1 - 1)
// Private Exponent mod (Prime2 - 1)
// Inverse of Prime2 mod Prime1
// PrivateExponent
const BCRYPT_RSAFULLPRIVATE_BLOB = 'RSAFULLPRIVATEBLOB';

const BCRYPT_RSAFULLPRIVATE_MAGIC = $33415352; // RSA3

//Properties of secret agreement algorithms
const BCRYPT_GLOBAL_PARAMETERS = 'SecretAgreementParam';
const BCRYPT_PRIVATE_KEY = 'PrivKeyVal';

// The BCRYPT_ECCPUBLIC_BLOB and BCRYPT_ECCPRIVATE_BLOB blob types are used
// to transport plaintext ECC keys. These blob types will be supported by
// all ECC primitive providers.
const BCRYPT_ECCPUBLIC_BLOB = 'ECCPUBLICBLOB';
const BCRYPT_ECCPRIVATE_BLOB = 'ECCPRIVATEBLOB';

const BCRYPT_ECCFULLPUBLIC_BLOB = 'ECCFULLPUBLICBLOB';
const BCRYPT_ECCFULLPRIVATE_BLOB = 'ECCFULLPRIVATEBLOB';
const SSL_ECCPUBLIC_BLOB = 'SSLECCPUBLICBLOB';
const TLS_13_PRE_SHARED_KEY = 'TLS13PRESHAREDKEY';
const TLS_KEM_CIPHERTEXT = 'TLS_KEM_CIPHER_TEXT';
const TLS_HYBRID_KEYEXCHANGE = 'TLS_HYBRID_KEYEXCHANGE';

const TLS_KEM_CIPHERTEXT_MAGIC = $434B4C4D; // MLKC
const TLS_HYBRID_KEYEXCHANGE_MAGIC = $48534C54; // TLSH

const BCRYPT_ECDH_PUBLIC_P256_MAGIC = $314B4345; // ECK1
const BCRYPT_ECDH_PRIVATE_P256_MAGIC = $324B4345; // ECK2
const BCRYPT_ECDH_PUBLIC_P384_MAGIC = $334B4345; // ECK3
const BCRYPT_ECDH_PRIVATE_P384_MAGIC = $344B4345; // ECK4
const BCRYPT_ECDH_PUBLIC_P521_MAGIC = $354B4345; // ECK5
const BCRYPT_ECDH_PRIVATE_P521_MAGIC = $364B4345; // ECK6

const BCRYPT_ECDH_PUBLIC_GENERIC_MAGIC = $504B4345; // ECKP
const BCRYPT_ECDH_PRIVATE_GENERIC_MAGIC = $564B4345; // ECKV

const BCRYPT_ECDSA_PUBLIC_P256_MAGIC = $31534345; // ECS1
const BCRYPT_ECDSA_PRIVATE_P256_MAGIC = $32534345; // ECS2
const BCRYPT_ECDSA_PUBLIC_P384_MAGIC = $33534345; // ECS3
const BCRYPT_ECDSA_PRIVATE_P384_MAGIC = $34534345; // ECS4
const BCRYPT_ECDSA_PUBLIC_P521_MAGIC = $35534345; // ECS5
const BCRYPT_ECDSA_PRIVATE_P521_MAGIC = $36534345; // ECS6

const BCRYPT_ECDSA_PUBLIC_GENERIC_MAGIC = $50444345; // ECDP
const BCRYPT_ECDSA_PRIVATE_GENERIC_MAGIC = $56444345; // ECDV

type
  _BCRYPT_ECCKEY_BLOB = record
    dwMagic: ULONG;
    cbKey: ULONG;
  end;
  BCRYPT_ECCKEY_BLOB = _BCRYPT_ECCKEY_BLOB;
  PBCRYPT_ECCKEY_BLOB = ^_BCRYPT_ECCKEY_BLOB;

//SSL ECC Public Blob Version
type
  _SSL_ECCKEY_BLOB = record
    dwCurveType: ULONG;
    cbKey: ULONG;
  end;
  SSL_ECCKEY_BLOB = _SSL_ECCKEY_BLOB;
  PSSL_ECCKEY_BLOB = ^_SSL_ECCKEY_BLOB;

type
  TLS_KEM_CIPHERTEXT_BLOB = record
    dwMagic: ULONG;
    cbLength: ULONG;
    // rgbCipherText[cbLength]
  end;
  PTLS_KEM_CIPHERTEXT_BLOB = ^TLS_KEM_CIPHERTEXT_BLOB;

const MAX_HYBRID_KEY_EXCHANGE_BLOBS = 4;
type
  TLS_HYBRID_KEYEXCHANGE_BLOB = record
    dwMagic: ULONG;
    dwKeyType: ULONG;
    NumBlobs: UCHAR;
    rgwszBlobTypes: array[0..MAX_HYBRID_KEY_EXCHANGE_BLOBS-1] of PCWSTR;
    rgbBlobs: array[0..MAX_HYBRID_KEY_EXCHANGE_BLOBS-1] of PUCHAR;
  end;
  PTLS_HYBRID_KEYEXCHANGE_BLOB = ^TLS_HYBRID_KEYEXCHANGE_BLOB;

//ECC Full versions
const BCRYPT_ECC_FULLKEY_BLOB_V1 = $1;

type
  ECC_CURVE_TYPE_ENUM = (
    BCRYPT_ECC_PRIME_SHORT_WEIERSTRASS_CURVE = $1,
    BCRYPT_ECC_PRIME_TWISTED_EDWARDS_CURVE = $2,
    BCRYPT_ECC_PRIME_MONTGOMERY_CURVE = $3);

type
  ECC_CURVE_ALG_ID_ENUM = (
    BCRYPT_NO_CURVE_GENERATION_ALG_ID = $0);

//The full version contains the curve parameters as well
//as the public and potentially private exponent.
type
  _BCRYPT_ECCFULLKEY_BLOB = record
    dwMagic: ULONG;
    dwVersion: ULONG; // Version of the structure
    dwCurveType: ECC_CURVE_TYPE_ENUM; // Supported curve types.
    dwCurveGenerationAlgId: ECC_CURVE_ALG_ID_ENUM; // For X.592 verification purposes, if we include Seed we will need to include the algorithm ID.
    cbFieldLength: ULONG; // Byte length of the fields P, A, B, X, Y.
    cbSubgroupOrder: ULONG; // Byte length of the subgroup.
    cbCofactor: ULONG; // Byte length of cofactor of G in E.
    cbSeed: ULONG; // Byte length of the seed used to generate the curve.
    // P[cbFieldLength] Prime specifying the base field.
    // A[cbFieldLength] Coefficient A of the equation y^2 = x^3 + A * x + B mod p
    // B[cbFieldLength] Coefficient B of the equation y^2 = x^3 + A * x + B mod p
    // Gx[cbFieldLength] X-coordinate of the base point.
    // Gy[cbFieldLength] Y-coordinate of the base point.
    // n[cbSubgroupOrder] Order of the group generated by G = (x,y)
    // h[cbCofactor] Cofactor of G in E.
    // S[cbSeed] Seed of the curve.
    // Qx[cbFieldLength] X-coordinate of the public point.
    // Qy[cbFieldLength] Y-coordinate of the public point.
    // d[cbSubgroupOrder] Private key. Not always present.
  end;
  BCRYPT_ECCFULLKEY_BLOB = _BCRYPT_ECCFULLKEY_BLOB;
  PBCRYPT_ECCFULLKEY_BLOB = ^_BCRYPT_ECCFULLKEY_BLOB;

// The BCRYPT_DH_PUBLIC_BLOB and BCRYPT_DH_PRIVATE_BLOB blob types are used
// to transport plaintext DH keys. These blob types will be supported by
// all DH primitive providers.
const BCRYPT_DH_PUBLIC_BLOB = 'DHPUBLICBLOB';
const BCRYPT_DH_PRIVATE_BLOB = 'DHPRIVATEBLOB';
const LEGACY_DH_PUBLIC_BLOB = 'CAPIDHPUBLICBLOB';
const LEGACY_DH_PRIVATE_BLOB = 'CAPIDHPRIVATEBLOB';

const BCRYPT_DH_PUBLIC_MAGIC = $42504844; // DHPB
const BCRYPT_DH_PRIVATE_MAGIC = $56504844; // DHPV

type
  _BCRYPT_DH_KEY_BLOB = record
    dwMagic: ULONG;
    cbKey: ULONG;
  end;
  BCRYPT_DH_KEY_BLOB = _BCRYPT_DH_KEY_BLOB;
  PBCRYPT_DH_KEY_BLOB = ^_BCRYPT_DH_KEY_BLOB;

// Property Strings for DH
const BCRYPT_DH_PARAMETERS = 'DHParameters';

const BCRYPT_DH_PARAMETERS_MAGIC = $4d504844; // DHPM

type
  _BCRYPT_DH_PARAMETER_HEADER = record
    cbLength: ULONG;
    dwMagic: ULONG;
    cbKeyLength: ULONG;
  end;
  BCRYPT_DH_PARAMETER_HEADER = _BCRYPT_DH_PARAMETER_HEADER;

// The BCRYPT_DSA_PUBLIC_BLOB and BCRYPT_DSA_PRIVATE_BLOB blob types are used
// to transport plaintext DSA keys. These blob types will be supported by
// all DSA primitive providers.
const BCRYPT_DSA_PUBLIC_BLOB = 'DSAPUBLICBLOB';
const BCRYPT_DSA_PRIVATE_BLOB = 'DSAPRIVATEBLOB';
const LEGACY_DSA_PUBLIC_BLOB = 'CAPIDSAPUBLICBLOB';
const LEGACY_DSA_PRIVATE_BLOB = 'CAPIDSAPRIVATEBLOB';
const LEGACY_DSA_V2_PUBLIC_BLOB = 'V2CAPIDSAPUBLICBLOB';
const LEGACY_DSA_V2_PRIVATE_BLOB = 'V2CAPIDSAPRIVATEBLOB';

const BCRYPT_DSA_PUBLIC_MAGIC = $42505344; // DSPB
const BCRYPT_DSA_PRIVATE_MAGIC = $56505344; // DSPV

const BCRYPT_DSA_PUBLIC_MAGIC_V2 = $32425044; // DPB2
const BCRYPT_DSA_PRIVATE_MAGIC_V2 = $32565044; // DPV2

type
  _BCRYPT_DSA_KEY_BLOB = record
    dwMagic: ULONG;
    cbKey: ULONG;
    Count: array[0..4-1] of UCHAR;
    Seed: array[0..20-1] of UCHAR;
    q: array[0..20-1] of UCHAR;
  end;
  BCRYPT_DSA_KEY_BLOB = _BCRYPT_DSA_KEY_BLOB;
  PBCRYPT_DSA_KEY_BLOB = ^_BCRYPT_DSA_KEY_BLOB;

type
  HASHALGORITHM_ENUM = (
    DSA_HASH_ALGORITHM_SHA1,
    DSA_HASH_ALGORITHM_SHA256,
    DSA_HASH_ALGORITHM_SHA512);

type
  DSAFIPSVERSION_ENUM = (
    DSA_FIPS186_2,
    DSA_FIPS186_3);

type
  _BCRYPT_DSA_KEY_BLOB_V2 = record
    dwMagic: ULONG;
    cbKey: ULONG;
    hashAlgorithm: HASHALGORITHM_ENUM;
    standardVersion: DSAFIPSVERSION_ENUM;
    cbSeedLength: ULONG;
    cbGroupSize: ULONG;
    Count: array[0..4-1] of UCHAR;
  end;
  BCRYPT_DSA_KEY_BLOB_V2 = _BCRYPT_DSA_KEY_BLOB_V2;
  PBCRYPT_DSA_KEY_BLOB_V2 = ^_BCRYPT_DSA_KEY_BLOB_V2;

type
  _BCRYPT_KEY_DATA_BLOB_HEADER = record
    dwMagic: ULONG;
    dwVersion: ULONG;
    cbKeyData: ULONG;
  end;
  BCRYPT_KEY_DATA_BLOB_HEADER = _BCRYPT_KEY_DATA_BLOB_HEADER;
  PBCRYPT_KEY_DATA_BLOB_HEADER = ^_BCRYPT_KEY_DATA_BLOB_HEADER;

const BCRYPT_KEY_DATA_BLOB_MAGIC = $4d42444b; // Key Data Blob Magic (KDBM)

const BCRYPT_KEY_DATA_BLOB_VERSION1 = $1;

const BCRYPT_MLDSA_PUBLIC_MAGIC = $4B505344; // DSPK
const BCRYPT_MLDSA_PRIVATE_MAGIC = $4B535344; // DSSK
const BCRYPT_MLDSA_PRIVATE_SEED_MAGIC = $53535344; // DSSS

const BCRYPT_SLHDSA_PUBLIC_MAGIC = $4B504853; // SHPK
const BCRYPT_SLHDSA_PRIVATE_MAGIC = $4B534853; // SHSK

const BCRYPT_LMS_PUBLIC_MAGIC = $4B504D4C; // LMPK
const BCRYPT_XMSS_PUBLIC_MAGIC = $4B504D58; // XMPK

const BCRYPT_PQDSA_PUBLIC_BLOB = 'PQDSAPUBLICBLOB';
const BCRYPT_PQDSA_PRIVATE_BLOB = 'PQDSAPRIVATEBLOB';
const BCRYPT_PQDSA_PRIVATE_SEED_BLOB = 'PQDSAPRIVATESEEDBLOB';

type
  BCRYPT_PQDSA_KEY_BLOB = record
    dwMagic: ULONG;
    cbParameterSet: ULONG; // Byte size of parameterSet[]
    cbKey: ULONG; // Byte size of key[]
    // WCHAR parameterSet[cbParameterSet / sizeof(WCHAR)]; // For ML-DSA and SLH-DSA, \0-terminated
    // BYTE key[cbKey]; // Key material
  end;
  PBCRYPT_PQDSA_KEY_BLOB = ^BCRYPT_PQDSA_KEY_BLOB;

const BCRYPT_MLKEM_PUBLIC_MAGIC = $504B4C4D; // MLKP
const BCRYPT_MLKEM_PRIVATE_MAGIC = $524B4C4D; // MLKR
const BCRYPT_MLKEM_PRIVATE_SEED_MAGIC = $534B4C4D; // MLKS

const BCRYPT_MLKEM_PUBLIC_BLOB = 'MLKEMPUBLICBLOB';
const BCRYPT_MLKEM_PRIVATE_BLOB = 'MLKEMPRIVATEBLOB';
const BCRYPT_MLKEM_PRIVATE_SEED_BLOB = 'MLKEMPRIVATESEEDBLOB';
const BCRYPT_MLKEM_ENCAPSULATION_BLOB = BCRYPT_MLKEM_PUBLIC_BLOB;
const BCRYPT_MLKEM_DECAPSULATION_BLOB = BCRYPT_MLKEM_PRIVATE_BLOB;

type
  BCRYPT_MLKEM_KEY_BLOB = record
    dwMagic: ULONG;
    cbParameterSet: ULONG; // Byte size of parameterSet[]
    cbKey: ULONG; // Byte size of key[]
    // WCHAR parameterSet[cbParameterSet / sizeof(WCHAR)]; // For ML-KEM, \0-terminated
    // BYTE key[cbKey]; // Key material
  end;
  PBCRYPT_MLKEM_KEY_BLOB = ^BCRYPT_MLKEM_KEY_BLOB;

// Property Strings for DSA
const BCRYPT_DSA_PARAMETERS = 'DSAParameters';

const BCRYPT_DSA_PARAMETERS_MAGIC = $4d505344; // DSPM

const BCRYPT_DSA_PARAMETERS_MAGIC_V2 = $324d5044; // DPM2

type
  _BCRYPT_DSA_PARAMETER_HEADER = record
    cbLength: ULONG;
    dwMagic: ULONG;
    cbKeyLength: ULONG;
    Count: array[0..4-1] of UCHAR;
    Seed: array[0..20-1] of UCHAR;
    q: array[0..20-1] of UCHAR;
  end;
  BCRYPT_DSA_PARAMETER_HEADER = _BCRYPT_DSA_PARAMETER_HEADER;

type
  _BCRYPT_DSA_PARAMETER_HEADER_V2 = record
    cbLength: ULONG;
    dwMagic: ULONG;
    cbKeyLength: ULONG;
    hashAlgorithm: HASHALGORITHM_ENUM;
    standardVersion: DSAFIPSVERSION_ENUM;
    cbSeedLength: ULONG;
    cbGroupSize: ULONG;
    Count: array[0..4-1] of UCHAR;
  end;
  BCRYPT_DSA_PARAMETER_HEADER_V2 = _BCRYPT_DSA_PARAMETER_HEADER_V2;

//Property Strings for ECC
const BCRYPT_ECC_PARAMETERS = 'ECCParameters';
const BCRYPT_ECC_CURVE_NAME = 'ECCCurveName';
const BCRYPT_ECC_CURVE_NAME_LIST = 'ECCCurveNameList';

const BCRYPT_ECC_PARAMETERS_MAGIC = $50434345; // ECCP

type
  _BCRYPT_ECC_CURVE_NAMES = record
    dwEccCurveNames: ULONG;
    pEccCurveNames: PLPWSTR;
  end;
  BCRYPT_ECC_CURVE_NAMES = _BCRYPT_ECC_CURVE_NAMES;

//
// ECC Curve Names
//
const BCRYPT_ECC_CURVE_BRAINPOOLP160R1 = 'brainpoolP160r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP160T1 = 'brainpoolP160t1';
const BCRYPT_ECC_CURVE_BRAINPOOLP192R1 = 'brainpoolP192r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP192T1 = 'brainpoolP192t1';
const BCRYPT_ECC_CURVE_BRAINPOOLP224R1 = 'brainpoolP224r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP224T1 = 'brainpoolP224t1';
const BCRYPT_ECC_CURVE_BRAINPOOLP256R1 = 'brainpoolP256r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP256T1 = 'brainpoolP256t1';
const BCRYPT_ECC_CURVE_BRAINPOOLP320R1 = 'brainpoolP320r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP320T1 = 'brainpoolP320t1';
const BCRYPT_ECC_CURVE_BRAINPOOLP384R1 = 'brainpoolP384r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP384T1 = 'brainpoolP384t1';
const BCRYPT_ECC_CURVE_BRAINPOOLP512R1 = 'brainpoolP512r1';
const BCRYPT_ECC_CURVE_BRAINPOOLP512T1 = 'brainpoolP512t1';

const BCRYPT_ECC_CURVE_25519 = 'curve25519';

const BCRYPT_ECC_CURVE_EC192WAPI = 'ec192wapi';

const BCRYPT_ECC_CURVE_NISTP192 = 'nistP192';
const BCRYPT_ECC_CURVE_NISTP224 = 'nistP224';
const BCRYPT_ECC_CURVE_NISTP256 = 'nistP256';
const BCRYPT_ECC_CURVE_NISTP384 = 'nistP384';
const BCRYPT_ECC_CURVE_NISTP521 = 'nistP521';

const BCRYPT_ECC_CURVE_NUMSP256T1 = 'numsP256t1';
const BCRYPT_ECC_CURVE_NUMSP384T1 = 'numsP384t1';
const BCRYPT_ECC_CURVE_NUMSP512T1 = 'numsP512t1';

const BCRYPT_ECC_CURVE_SECP160K1 = 'secP160k1';
const BCRYPT_ECC_CURVE_SECP160R1 = 'secP160r1';
const BCRYPT_ECC_CURVE_SECP160R2 = 'secP160r2';
const BCRYPT_ECC_CURVE_SECP192K1 = 'secP192k1';
const BCRYPT_ECC_CURVE_SECP192R1 = 'secP192r1';
const BCRYPT_ECC_CURVE_SECP224K1 = 'secP224k1';
const BCRYPT_ECC_CURVE_SECP224R1 = 'secP224r1';
const BCRYPT_ECC_CURVE_SECP256K1 = 'secP256k1';
const BCRYPT_ECC_CURVE_SECP256R1 = 'secP256r1';
const BCRYPT_ECC_CURVE_SECP384R1 = 'secP384r1';
const BCRYPT_ECC_CURVE_SECP521R1 = 'secP521r1';

const BCRYPT_ECC_CURVE_WTLS7 = 'wtls7';
const BCRYPT_ECC_CURVE_WTLS9 = 'wtls9';
const BCRYPT_ECC_CURVE_WTLS12 = 'wtls12';

const BCRYPT_ECC_CURVE_X962P192V1 = 'x962P192v1';
const BCRYPT_ECC_CURVE_X962P192V2 = 'x962P192v2';
const BCRYPT_ECC_CURVE_X962P192V3 = 'x962P192v3';
const BCRYPT_ECC_CURVE_X962P239V1 = 'x962P239v1';
const BCRYPT_ECC_CURVE_X962P239V2 = 'x962P239v2';
const BCRYPT_ECC_CURVE_X962P239V3 = 'x962P239v3';
const BCRYPT_ECC_CURVE_X962P256V1 = 'x962P256v1';

//
// PQDSA property values for BCRYPT_PARAMETER_SET_NAME
//
const BCRYPT_MLDSA_PARAMETER_SET_44 = '44';
const BCRYPT_MLDSA_PARAMETER_SET_65 = '65';
const BCRYPT_MLDSA_PARAMETER_SET_87 = '87';

const BCRYPT_SLHDSA_PARAMETER_SET_SHA2_128S = 'SHA2-128s';
const BCRYPT_SLHDSA_PARAMETER_SET_SHAKE_128S = 'SHAKE-128s';
const BCRYPT_SLHDSA_PARAMETER_SET_SHA2_128F = 'SHA2-128f';
const BCRYPT_SLHDSA_PARAMETER_SET_SHAKE_128F = 'SHAKE-128f';
const BCRYPT_SLHDSA_PARAMETER_SET_SHA2_192S = 'SHA2-192s';
const BCRYPT_SLHDSA_PARAMETER_SET_SHAKE_192S = 'SHAKE-192s';
const BCRYPT_SLHDSA_PARAMETER_SET_SHA2_192F = 'SHA2-192f';
const BCRYPT_SLHDSA_PARAMETER_SET_SHAKE_192F = 'SHAKE-192f';
const BCRYPT_SLHDSA_PARAMETER_SET_SHA2_256S = 'SHA2-256s';
const BCRYPT_SLHDSA_PARAMETER_SET_SHAKE_256S = 'SHAKE-256s';
const BCRYPT_SLHDSA_PARAMETER_SET_SHA2_256F = 'SHA2-256f';
const BCRYPT_SLHDSA_PARAMETER_SET_SHAKE_256F = 'SHAKE-256f';

//
// MLKEM property values for BCRYPT_PARAMETER_SET_NAME
//
const BCRYPT_MLKEM_PARAMETER_SET_512 = '512';
const BCRYPT_MLKEM_PARAMETER_SET_768 = '768';
const BCRYPT_MLKEM_PARAMETER_SET_1024 = '1024';

// Operation types used in BCRYPT_MULTI_HASH_OPERATION structures
type
  BCRYPT_HASH_OPERATION_TYPE = (
    BCRYPT_HASH_OPERATION_HASH_DATA = 1,
    BCRYPT_HASH_OPERATION_FINISH_HASH = 2);

type
  _BCRYPT_MULTI_HASH_OPERATION = record
    iHash: ULONG; // index of hash object
    hashOperation: BCRYPT_HASH_OPERATION_TYPE; // operation to be performed
    pbBuffer: PUCHAR; // data to be hashed, or result buffer
    cbBuffer: ULONG;
  end;
  BCRYPT_MULTI_HASH_OPERATION = _BCRYPT_MULTI_HASH_OPERATION;

// Enum to specify type of multi-operation is passed to BCryptProcesMultiOperations
type
  BCRYPT_MULTI_OPERATION_TYPE = (
    BCRYPT_OPERATION_TYPE_HASH = 1); // structure type is BCRYPT_MULTI_HASH_OPERATION

type
  _BCRYPT_MULTI_OBJECT_LENGTH_STRUCT = record
    cbPerObject: ULONG;
    cbPerElement: ULONG; // required size for N elements is (cbPerObject + N * cbPerElement)
  end;
  BCRYPT_MULTI_OBJECT_LENGTH_STRUCT = _BCRYPT_MULTI_OBJECT_LENGTH_STRUCT;

//
// Microsoft built-in providers.
//

const MS_PRIMITIVE_PROVIDER = 'Microsoft Primitive Provider';
const MS_PLATFORM_CRYPTO_PROVIDER = 'Microsoft Platform Crypto Provider';

//
// Common algorithm identifiers.
//

const BCRYPT_RSA_ALGORITHM = 'RSA';
const BCRYPT_RSA_SIGN_ALGORITHM = 'RSA_SIGN';
const BCRYPT_DH_ALGORITHM = 'DH';
const BCRYPT_DSA_ALGORITHM = 'DSA';
const BCRYPT_RC2_ALGORITHM = 'RC2';
const BCRYPT_RC4_ALGORITHM = 'RC4';
const BCRYPT_AES_ALGORITHM = 'AES';
const BCRYPT_DES_ALGORITHM = 'DES';
const BCRYPT_DESX_ALGORITHM = 'DESX';
const BCRYPT_3DES_ALGORITHM = '3DES';
const BCRYPT_3DES_112_ALGORITHM = '3DES_112';
const BCRYPT_MD2_ALGORITHM = 'MD2';
const BCRYPT_MD4_ALGORITHM = 'MD4';
const BCRYPT_MD5_ALGORITHM = 'MD5';
const BCRYPT_SHA1_ALGORITHM = 'SHA1';
const BCRYPT_SHA256_ALGORITHM = 'SHA256';
const BCRYPT_SHA384_ALGORITHM = 'SHA384';
const BCRYPT_SHA512_ALGORITHM = 'SHA512';
const BCRYPT_AES_GMAC_ALGORITHM = 'AES-GMAC';
const BCRYPT_AES_CMAC_ALGORITHM = 'AES-CMAC';
const BCRYPT_ECDSA_P256_ALGORITHM = 'ECDSA_P256';
const BCRYPT_ECDSA_P384_ALGORITHM = 'ECDSA_P384';
const BCRYPT_ECDSA_P521_ALGORITHM = 'ECDSA_P521';
const BCRYPT_ECDH_P256_ALGORITHM = 'ECDH_P256';
const BCRYPT_ECDH_P384_ALGORITHM = 'ECDH_P384';
const BCRYPT_ECDH_P521_ALGORITHM = 'ECDH_P521';
const BCRYPT_RNG_ALGORITHM = 'RNG';
const BCRYPT_RNG_FIPS186_DSA_ALGORITHM = 'FIPS186DSARNG';
const BCRYPT_RNG_DUAL_EC_ALGORITHM = 'DUALECRNG';

const BCRYPT_SP800108_CTR_HMAC_ALGORITHM = 'SP800_108_CTR_HMAC';
const BCRYPT_SP80056A_CONCAT_ALGORITHM = 'SP800_56A_CONCAT';
const BCRYPT_PBKDF2_ALGORITHM = 'PBKDF2';
const BCRYPT_CAPI_KDF_ALGORITHM = 'CAPI_KDF';
const BCRYPT_TLS1_1_KDF_ALGORITHM = 'TLS1_1_KDF';
const BCRYPT_TLS1_2_KDF_ALGORITHM = 'TLS1_2_KDF';

const BCRYPT_ECDSA_ALGORITHM = 'ECDSA';
const BCRYPT_ECDH_ALGORITHM = 'ECDH';
const BCRYPT_XTS_AES_ALGORITHM = 'XTS-AES';

const BCRYPT_HKDF_ALGORITHM = 'HKDF';

const BCRYPT_CHACHA20_POLY1305_ALGORITHM = 'CHACHA20_POLY1305';

const BCRYPT_SHA3_256_ALGORITHM = 'SHA3-256';
const BCRYPT_SHA3_384_ALGORITHM = 'SHA3-384';
const BCRYPT_SHA3_512_ALGORITHM = 'SHA3-512';
const BCRYPT_CSHAKE128_ALGORITHM = 'CSHAKE128';
const BCRYPT_CSHAKE256_ALGORITHM = 'CSHAKE256';
const BCRYPT_KMAC128_ALGORITHM = 'KMAC128';
const BCRYPT_KMAC256_ALGORITHM = 'KMAC256';

const BCRYPT_SHAKE128_ALGORITHM = 'SHAKE128';
const BCRYPT_SHAKE256_ALGORITHM = 'SHAKE256';

const BCRYPT_MLDSA_ALGORITHM = 'ML-DSA';
const BCRYPT_SLHDSA_ALGORITHM = 'SLH-DSA';

const BCRYPT_LMS_ALGORITHM = 'LMS';
const BCRYPT_XMSS_ALGORITHM = 'XMSS';

const BCRYPT_MLKEM_ALGORITHM = 'ML-KEM';

//
// Interfaces
//

const BCRYPT_CIPHER_INTERFACE = $00000001;
const BCRYPT_HASH_INTERFACE = $00000002;
const BCRYPT_ASYMMETRIC_ENCRYPTION_INTERFACE = $00000003;
const BCRYPT_SECRET_AGREEMENT_INTERFACE = $00000004;
const BCRYPT_SIGNATURE_INTERFACE = $00000005;
const BCRYPT_RNG_INTERFACE = $00000006;

const BCRYPT_KEY_DERIVATION_INTERFACE = $00000007;

const BCRYPT_KEY_ENCAPSULATION_INTERFACE = $00000008;

//
// Algorithm pseudo-handles
// Pseudo-handles are distinguished from normal handles by being 1 mod 16
//
const BCRYPT_MD2_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000001);
const BCRYPT_MD4_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000011);
const BCRYPT_MD5_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000021);
const BCRYPT_SHA1_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000031);
const BCRYPT_SHA256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000041);
const BCRYPT_SHA384_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000051);
const BCRYPT_SHA512_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000061);
const BCRYPT_RC4_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000071);
const BCRYPT_RNG_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000081);
const BCRYPT_HMAC_MD5_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000091);
const BCRYPT_HMAC_SHA1_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000000a1);
const BCRYPT_HMAC_SHA256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000000b1);
const BCRYPT_HMAC_SHA384_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000000c1);
const BCRYPT_HMAC_SHA512_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000000d1);
const BCRYPT_RSA_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000000e1);
const BCRYPT_ECDSA_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000000f1);

const BCRYPT_AES_CMAC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000101);
const BCRYPT_AES_GMAC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000111);
const BCRYPT_HMAC_MD2_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000121);
const BCRYPT_HMAC_MD4_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000131);

const BCRYPT_3DES_CBC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000141);
const BCRYPT_3DES_ECB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000151);
const BCRYPT_3DES_CFB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000161);
const BCRYPT_3DES_112_CBC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000171);
const BCRYPT_3DES_112_ECB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000181);
const BCRYPT_3DES_112_CFB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000191);
const BCRYPT_AES_CBC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000001a1);
const BCRYPT_AES_ECB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000001b1);
const BCRYPT_AES_CFB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000001c1);
const BCRYPT_AES_CCM_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000001d1);
const BCRYPT_AES_GCM_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000001e1);
const BCRYPT_DES_CBC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000001f1);
const BCRYPT_DES_ECB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000201);
const BCRYPT_DES_CFB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000211);
const BCRYPT_DESX_CBC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000221);
const BCRYPT_DESX_ECB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000231);
const BCRYPT_DESX_CFB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000241);
const BCRYPT_RC2_CBC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000251);
const BCRYPT_RC2_ECB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000261);
const BCRYPT_RC2_CFB_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000271);

const BCRYPT_DH_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000281);
const BCRYPT_ECDH_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000291);
const BCRYPT_ECDH_P256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000002a1);
const BCRYPT_ECDH_P384_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000002b1);
const BCRYPT_ECDH_P521_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000002c1);
const BCRYPT_DSA_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000002d1);
const BCRYPT_ECDSA_P256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000002e1);
const BCRYPT_ECDSA_P384_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000002f1);
const BCRYPT_ECDSA_P521_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000301);
const BCRYPT_RSA_SIGN_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000311);

const BCRYPT_CAPI_KDF_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000321);
const BCRYPT_PBKDF2_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000331);

const BCRYPT_SP800108_CTR_HMAC_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000341);
const BCRYPT_SP80056A_CONCAT_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000351);

const BCRYPT_TLS1_1_KDF_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000361);
const BCRYPT_TLS1_2_KDF_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000371);

const BCRYPT_XTS_AES_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000381);

const BCRYPT_HKDF_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000391);

const BCRYPT_CHACHA20_POLY1305_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000003A1);

const BCRYPT_SHA3_256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000003B1);
const BCRYPT_SHA3_384_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000003C1);
const BCRYPT_SHA3_512_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000003D1);
const BCRYPT_HMAC_SHA3_256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000003E1);
const BCRYPT_HMAC_SHA3_384_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($000003F1);
const BCRYPT_HMAC_SHA3_512_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000401);
const BCRYPT_CSHAKE128_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000411);
const BCRYPT_CSHAKE256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000421);
const BCRYPT_KMAC128_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000431);
const BCRYPT_KMAC256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000441);

const BCRYPT_SHAKE128_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000451);
const BCRYPT_SHAKE256_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000461);

const BCRYPT_MLDSA_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000471);

const BCRYPT_MLKEM_ALG_HANDLE: BCRYPT_ALG_HANDLE = BCRYPT_ALG_HANDLE($00000481);

//
// Primitive algorithm provider functions.
//

const BCRYPT_ALG_HANDLE_HMAC_FLAG = $00000008;
const BCRYPT_HASH_REUSABLE_FLAG = $00000020;

const BCRYPT_CAPI_AES_FLAG = $00000010;

const BCRYPT_MULTI_FLAG = $00000040;

//
// Extendable-output functions (XOFs) allow generating output multiple times from their
// state. BCRYPT_HASH_DONT_RESET_FLAG allows to override the default behavior of BCryptFinishHash,
// which is to reset the hash state. If this flag is set, the hash state is not reset and
// users may invoke BCryptFinishHash to generate more data out of the hash/XOF state, until
// after a BCryptFinishHash call where this flag is unset.
//
const BCRYPT_HASH_DONT_RESET_FLAG = $00000001;

//
// The TLS_CBC_HMAC_VERIFY flag provides a side-channel safe way of verifying TLS data records
// from the CBC-HMAC cipher suites. See RFC 5246 section 6.2.3.2.
// This flag is used in BCryptOpenAlgorithmProvider and in BCryptHashData.
// For BCryptOpenAlgorithmProvider it ensures that you get a provider that supports this feature.
// For BCryptHashData is changes the functionality.
// The Input buffer now contains the whole TLS data record, consisting of the plaintext,
// followed by the MAC value, followed by the padding, followed by the padding_length.
// The function will compute the HMAC over the data already hashed plus the plaintext,
// compare it to the MAC value, and verify that the padding is correct.
// If all works out, it returns a success value; if anything fails it returns an error.
// What makes this special is that the code path or the memory access pattern used to
// do this verification does not depend on padding_length to stop attacks on the CBC encryption
// that was used to decrypt this data.
// This flag is only useful for TLS implementations, other callers should not use it.
// This flag is only valid for HMAC-SHA1, HMAC-SHA256, and HMAC-SHA384.
// This flag implies the BCRYPT_HASH_REUSABLE_FLAG.
//
// This flag is available staring in Windows 10 19H1, but we define it for all
// NTDDI values to allow applications to dynamically test wethere the OS supports the
// feature, and adjust accordingly.
//
const BCRYPT_TLS_CBC_HMAC_VERIFY_FLAG = $00000004;

//
// The BUFFERS_LOCKED flag used in BCryptEncrypt/BCryptDecrypt signals that
// the pbInput and pbOutput buffers have been locked (see MmProbeAndLockPages)
// and CNG may not lock the buffers again.
// This flag applies only to kernel mode, it is ignored in user mode.
//
const BCRYPT_BUFFERS_LOCKED_FLAG = $00000040;

//
// The EXTENDED_KEYSIZE flag extends the supported set of key sizes.
//
// The original design has a per-algorithm maximum key size, and
// BCryptGenerateSymmetricKey truncates any longer input to the maximum key size for that
// algorithm. Some callers depend on this feature and pass in large buffers.
// This makes it impossible to silently extend the supported key size without breaking
// backward compatibility.
// This flag indicates that the extended key size support is requested.
// It has the following consequences:
// - BCryptGetProperty will report a new maximum key size for BCRYPT_KEY_LENGTHS.
// - BCryptGenerateSymmetricKey will support the longer key sizes.
// - BCryptGenerateSymmetricKey will no longer truncate keys that are too long, but return an error instead.
//
const BCRYPT_EXTENDED_KEYSIZE = $00000080;

//
// ENABLE_INCOMPATIBLE_FIPS_CHECKS flag enables some FIPS 140-2-mandated checks that are incompatible
// with the original algorithm.
//
// Starting in Redstone 1 (summer 2016 release of Win10) this flag has the following effect on the
//  Microsoft default algorithm provider.
// - BCryptGenerateSymmetricKey when generating an XTS-AES key with this flag specified and FIPS mode enabled
//      will verify that the two halves of the key are not identical. If they are, an error is returned.
//      This is actually incompatible with the NIST SP 800-38E and IEEE Std 1619-2007 definitions
//      of XTS-AES. Rather than change the standard, NIST added this requirement in the FIPS 140-2
//      implementation guidance.
//      This check breaks existing usage of the algorithm, which is why we only perform the check when the
//      caller explicitly asks for it.
//      Use of this flag  for any algorithm other than XTS-AES generates an error.
// Note that this flag is not supported for BCryptImportKey.
//
const BCRYPT_ENABLE_INCOMPATIBLE_FIPS_CHECKS = $00000100;

function BCryptOpenAlgorithmProvider(
           phAlgorithm: PBCRYPT_ALG_HANDLE;
           pszAlgId: LPCWSTR;
           pszImplementation: LPCWSTR;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

// AlgOperations flags for use with BCryptEnumAlgorithms()
const BCRYPT_CIPHER_OPERATION = $00000001;
const BCRYPT_HASH_OPERATION = $00000002;
const BCRYPT_ASYMMETRIC_ENCRYPTION_OPERATION = $00000004;
const BCRYPT_SECRET_AGREEMENT_OPERATION = $00000008;
const BCRYPT_SIGNATURE_OPERATION = $00000010;
const BCRYPT_RNG_OPERATION = $00000020;

const BCRYPT_KEY_DERIVATION_OPERATION = $00000040;

const BCRYPT_KEY_ENCAPSULATION_OPERATION = $00000080;

// USE EXTREME CAUTION: editing comments that contain "certenrolls_*" tokens
// could break building CertEnroll idl files:
// certenrolls_begin -- BCRYPT_ALGORITHM_IDENTIFIER
type
  _BCRYPT_ALGORITHM_IDENTIFIER = record
    pszName: LPWSTR;
    dwClass: ULONG;
    dwFlags: ULONG;
  end;
  BCRYPT_ALGORITHM_IDENTIFIER = _BCRYPT_ALGORITHM_IDENTIFIER;
  PBCRYPT_ALGORITHM_IDENTIFIER = ^BCRYPT_ALGORITHM_IDENTIFIER;
  PPBCRYPT_ALGORITHM_IDENTIFIER = ^PBCRYPT_ALGORITHM_IDENTIFIER;
// certenrolls_end

function BCryptEnumAlgorithms(
           dwAlgOperations: ULONG;
           pAlgCount: PULONG;
           ppAlgList: PPBCRYPT_ALGORITHM_IDENTIFIER;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

type
  _BCRYPT_PROVIDER_NAME = record
    pszProviderName: LPWSTR;
  end;
  BCRYPT_PROVIDER_NAME = _BCRYPT_PROVIDER_NAME;
  PBCRYPT_PROVIDER_NAME = ^BCRYPT_PROVIDER_NAME;
  PPBCRYPT_PROVIDER_NAME = ^PBCRYPT_PROVIDER_NAME;

function BCryptEnumProviders(
           pszAlgId: LPCWSTR;
           pImplCount: PULONG;
           ppImplList: PPBCRYPT_PROVIDER_NAME;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

// Unused flags. Kept for backward compatibility.
//   "Flags for use with BCryptGetProperty and BCryptSetProperty"
const BCRYPT_PUBLIC_KEY_FLAG = $00000001;
const BCRYPT_PRIVATE_KEY_FLAG = $00000002;

function BCryptGetProperty(
           hObject: BCRYPT_HANDLE;
           pszProperty: LPCWSTR;
           pbOutput: PUCHAR;
           cbOutput: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptSetProperty(
           hObject: BCRYPT_HANDLE;
           pszProperty: LPCWSTR;
           pbInput: PUCHAR;
           cbInput: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptCloseAlgorithmProvider(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

procedure BCryptFreeBuffer(
            pvBuffer: PVOID); stdcall; external 'bcrypt.dll';

//
// Primitive encryption functions.
//
function BCryptGenerateSymmetricKey(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           phKey: PBCRYPT_KEY_HANDLE;
           pbKeyObject: PUCHAR;
           cbKeyObject: ULONG;
           pbSecret: PUCHAR;
           cbSecret: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptGenerateKeyPair(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           phKey: PBCRYPT_KEY_HANDLE;
           dwLength: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptEncrypt(
           hKey: BCRYPT_KEY_HANDLE;
           pbInput: PUCHAR;
           cbInput: ULONG;
           pPaddingInfo: PVOID;
           pbIV: PUCHAR;
           cbIV: ULONG;
           pbOutput: PUCHAR;
           cbOutput: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDecrypt(
           hKey: BCRYPT_KEY_HANDLE;
           pbInput: PUCHAR;
           cbInput: ULONG;
           pPaddingInfo: PVOID;
           pbIV: PUCHAR;
           cbIV: ULONG;
           pbOutput: PUCHAR;
           cbOutput: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptExportKey(
           hKey: BCRYPT_KEY_HANDLE;
           hExportKey: BCRYPT_KEY_HANDLE;
           pszBlobType: LPCWSTR;
           pbOutput: PUCHAR;
           cbOutput: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptImportKey(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           hImportKey: BCRYPT_KEY_HANDLE;
           pszBlobType: LPCWSTR;
           phKey: PBCRYPT_KEY_HANDLE;
           pbKeyObject: PUCHAR;
           cbKeyObject: ULONG;
           pbInput: PUCHAR;
           cbInput: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

const BCRYPT_NO_KEY_VALIDATION = $00000008;

const BCRYPT_KEY_VALIDATION_RANGE = $00000010; // BCryptImportKeyPair
const BCRYPT_KEY_VALIDATION_RANGE_AND_ORDER = $00000018; // BCryptImportKeyPair & BCryptFinalizeKeyPair
const BCRYPT_KEY_VALIDATION_REGENERATE = $00000020; // BCryptImportKeyPair

function BCryptImportKeyPair(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           hImportKey: BCRYPT_KEY_HANDLE;
           pszBlobType: LPCWSTR;
           phKey: PBCRYPT_KEY_HANDLE;
           pbInput: PUCHAR;
           cbInput: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDuplicateKey(
           hKey: BCRYPT_KEY_HANDLE;
           phNewKey: PBCRYPT_KEY_HANDLE;
           pbKeyObject: PUCHAR;
           cbKeyObject: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptFinalizeKeyPair(
           hKey: BCRYPT_KEY_HANDLE;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDestroyKey(
           hKey: BCRYPT_KEY_HANDLE): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDestroySecret(
           hSecret: BCRYPT_SECRET_HANDLE): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptSignHash(
           hKey: BCRYPT_KEY_HANDLE;
           pPaddingInfo: PVOID;
           pbInput: PUCHAR;
           cbInput: ULONG;
           pbOutput: PUCHAR;
           cbOutput: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptVerifySignature(
           hKey: BCRYPT_KEY_HANDLE;
           pPaddingInfo: PVOID;
           pbHash: PUCHAR;
           cbHash: ULONG;
           pbSignature: PUCHAR;
           cbSignature: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptSecretAgreement(
           hPrivKey: BCRYPT_KEY_HANDLE;
           hPubKey: BCRYPT_KEY_HANDLE;
           phAgreedSecret: PBCRYPT_SECRET_HANDLE;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDeriveKey(
           hSharedSecret: BCRYPT_SECRET_HANDLE;
           pwszKDF: LPCWSTR;
           pParameterList: PBCryptBufferDesc;
           pbDerivedKey: PUCHAR;
           cbDerivedKey: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptKeyDerivation(
           hKey: BCRYPT_KEY_HANDLE;
           pParameterList: PBCryptBufferDesc;
           pbDerivedKey: PUCHAR;
           cbDerivedKey: ULONG;
           pcbResult: PULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Primitive hashing functions.
//
function BCryptCreateHash(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           phHash: PBCRYPT_HASH_HANDLE;
           pbHashObject: PUCHAR;
           cbHashObject: ULONG;
           pbSecret: PUCHAR; // optional
           cbSecret: ULONG; // optional
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptHashData(
           hHash: BCRYPT_HASH_HANDLE;
           pbInput: PUCHAR;
           cbInput: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptFinishHash(
           hHash: BCRYPT_HASH_HANDLE;
           pbOutput: PUCHAR;
           cbOutput: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptCreateMultiHash(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           phHash: PBCRYPT_HASH_HANDLE;
           nHashes: ULONG;
           pbHashObject: PUCHAR;
           cbHashObject: ULONG;
           pbSecret: PUCHAR; // optional
           cbSecret: ULONG; // optional
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptProcessMultiOperations(
           hObject: BCRYPT_HANDLE;
           operationType: BCRYPT_MULTI_OPERATION_TYPE;
           pOperations: PVOID;
           cbOperations: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDuplicateHash(
           hHash: BCRYPT_HASH_HANDLE;
           phNewHash: PBCRYPT_HASH_HANDLE;
           pbHashObject: PUCHAR;
           cbHashObject: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDestroyHash(
           hHash: BCRYPT_HASH_HANDLE): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptHash(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           pbSecret: PUCHAR; // for keyed algs
           cbSecret: ULONG; // for keyed algs
           pbInput: PUCHAR;
           cbInput: ULONG;
           pbOutput: PUCHAR;
           cbOutput: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Primitive random number generation.
//

// Flags to BCryptGenRandom
// BCRYPT_RNG_USE_ENTROPY_IN_BUFFER is ignored in Win7 & later
const BCRYPT_RNG_USE_ENTROPY_IN_BUFFER = $00000001;
const BCRYPT_USE_SYSTEM_PREFERRED_RNG = $00000002;

function BCryptGenRandom(
           hAlgorithm: BCRYPT_ALG_HANDLE;
           pbBuffer: PUCHAR;
           cbBuffer: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Primitive key derivation functions.
//
function BCryptDeriveKeyCapi(
           hHash: BCRYPT_HASH_HANDLE;
           hTargetAlg: BCRYPT_ALG_HANDLE;
           pbDerivedKey: PUCHAR;
           cbDerivedKey: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptDeriveKeyPBKDF2(
           hPrf: BCRYPT_ALG_HANDLE;
           pbPassword: PUCHAR;
           cbPassword: ULONG;
           pbSalt: PUCHAR;
           cbSalt: ULONG;
           cIterations: ULONGLONG;
           pbDerivedKey: PUCHAR;
           cbDerivedKey: ULONG;
           dwFlags: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

// NTSTATUS
// __CALLCONV__
// BCryptEncapsulate(
// BCRYPT_KEY_HANDLE hKey,

// PUCHAR pbSecretKey,
// ULONG cbSecretKey,
// ULONG * pcbSecretKey,

// PUCHAR pbCipherText,
// ULONG cbCipherText,
// ULONG * pcbCipherText,
// ULONG dwFlags);

// NTSTATUS
// __CALLCONV__
// BCryptDecapsulate(
// BCRYPT_KEY_HANDLE hKey,

// PUCHAR pbCipherText,
// ULONG cbCipherText,

// PUCHAR pbSecretKey,
// ULONG cbSecretKey,
// ULONG * pcbSecretKey,
// ULONG dwFlags);

//
// Interface version control...
//
type
  _BCRYPT_INTERFACE_VERSION = record
    MajorVersion: USHORT;
    MinorVersion: USHORT;
  end;
  BCRYPT_INTERFACE_VERSION = _BCRYPT_INTERFACE_VERSION;
  PBCRYPT_INTERFACE_VERSION = ^_BCRYPT_INTERFACE_VERSION;

// #define BCRYPT_MAKE_INTERFACE_VERSION(major,minor) {(USHORT)major, (USHORT)minor}

// #define BCRYPT_IS_INTERFACE_VERSION_COMPATIBLE(loader, provider) ((loader).MajorVersion <= (provider).MajorVersion)

//
// Primitive provider interfaces.
//

// #define BCRYPT_CIPHER_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

// #define BCRYPT_HASH_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

const BCRYPT_HASH_INTERFACE_MAJORVERSION_2 = 2;
// #define BCRYPT_HASH_INTERFACE_VERSION_2 BCRYPT_MAKE_INTERFACE_VERSION(BCRYPT_HASH_INTERFACE_MAJORVERSION_2,0)

// #define BCRYPT_ASYMMETRIC_ENCRYPTION_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

// #define BCRYPT_SECRET_AGREEMENT_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

// #define BCRYPT_SIGNATURE_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

// #define BCRYPT_RNG_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

// #define BCRYPT_KEY_ENCAPSULATION_INTERFACE_VERSION_1 BCRYPT_MAKE_INTERFACE_VERSION(1,0)

//////////////////////////////////////////////////////////////////////////////
// CryptoConfig Definitions //////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

// Interface registration flags
const CRYPT_MIN_DEPENDENCIES = ($00000001);
const CRYPT_PROCESS_ISOLATE = ($00010000); // User-mode only

// Processor modes supported by a provider
//
// (Valid for BCryptQueryProviderRegistration and BCryptResolveProviders):
//
const CRYPT_UM = ($00000001); // User mode only
const CRYPT_KM = ($00000002); // Kernel mode only
const CRYPT_MM = ($00000003); // Multi-mode: Must support BOTH UM and KM
//
// (Valid only for BCryptQueryProviderRegistration):
//
const CRYPT_ANY = ($00000004); // Wildcard: Either UM, or KM, or both

// Write behavior flags
const CRYPT_OVERWRITE = ($00000001);

// Configuration tables
const CRYPT_LOCAL = ($00000001);
const CRYPT_DOMAIN = ($00000002);

// Context configuration flags
const CRYPT_EXCLUSIVE = ($00000001);
const CRYPT_OVERRIDE = ($00010000); // Enterprise table only

// Resolution and enumeration flags
const CRYPT_ALL_FUNCTIONS = ($00000001);
const CRYPT_ALL_PROVIDERS = ($00000002);

// Priority list positions
const CRYPT_PRIORITY_TOP = ($00000000);
const CRYPT_PRIORITY_BOTTOM = ($FFFFFFFF);

// Default system-wide context
const CRYPT_DEFAULT_CONTEXT = 'Default';

//////////////////////////////////////////////////////////////////////////////
// CryptoConfig Structures ///////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

//
// Provider Registration Structures
//

type
  _CRYPT_INTERFACE_REG = record
    dwInterface: ULONG;
    dwFlags: ULONG;
    cFunctions: ULONG;
    rgpszFunctions: PPWSTR;
  end;
  CRYPT_INTERFACE_REG = _CRYPT_INTERFACE_REG;
  PCRYPT_INTERFACE_REG = ^_CRYPT_INTERFACE_REG;
  PPCRYPT_INTERFACE_REG = ^PCRYPT_INTERFACE_REG;

type
  _CRYPT_IMAGE_REG = record
    pszImage: PWSTR;
    cInterfaces: ULONG;
    rgpInterfaces: PPCRYPT_INTERFACE_REG;
  end;
  CRYPT_IMAGE_REG = _CRYPT_IMAGE_REG;
  PCRYPT_IMAGE_REG = ^_CRYPT_IMAGE_REG;

type
  _CRYPT_PROVIDER_REG = record
    cAliases: ULONG;
    rgpszAliases: PPWSTR;
    pUM: PCRYPT_IMAGE_REG;
    pKM: PCRYPT_IMAGE_REG;
  end;
  CRYPT_PROVIDER_REG = _CRYPT_PROVIDER_REG;
  PCRYPT_PROVIDER_REG = ^_CRYPT_PROVIDER_REG;
  PPCRYPT_PROVIDER_REG = ^PCRYPT_PROVIDER_REG;

type
  _CRYPT_PROVIDERS = record
    cProviders: ULONG;
    rgpszProviders: PPWSTR;
  end;
  CRYPT_PROVIDERS = _CRYPT_PROVIDERS;
  PCRYPT_PROVIDERS = ^_CRYPT_PROVIDERS;
  PPCRYPT_PROVIDERS = ^PCRYPT_PROVIDERS;

//
// Context Configuration Structures
//

type
  _CRYPT_CONTEXT_CONFIG = record
    dwFlags: ULONG;
    dwReserved: ULONG;
  end;
  CRYPT_CONTEXT_CONFIG = _CRYPT_CONTEXT_CONFIG;
  PCRYPT_CONTEXT_CONFIG = ^_CRYPT_CONTEXT_CONFIG;
  PPCRYPT_CONTEXT_CONFIG = ^PCRYPT_CONTEXT_CONFIG;

type
  _CRYPT_CONTEXT_FUNCTION_CONFIG = record
    dwFlags: ULONG;
    dwReserved: ULONG;
  end;
  CRYPT_CONTEXT_FUNCTION_CONFIG = _CRYPT_CONTEXT_FUNCTION_CONFIG;
  PCRYPT_CONTEXT_FUNCTION_CONFIG = ^_CRYPT_CONTEXT_FUNCTION_CONFIG;
  PPCRYPT_CONTEXT_FUNCTION_CONFIG = ^PCRYPT_CONTEXT_FUNCTION_CONFIG;

type
  _CRYPT_CONTEXTS = record
    cContexts: ULONG;
    rgpszContexts: PPWSTR;
  end;
  CRYPT_CONTEXTS = _CRYPT_CONTEXTS;
  PCRYPT_CONTEXTS = ^_CRYPT_CONTEXTS;
  PPCRYPT_CONTEXTS = ^PCRYPT_CONTEXTS;

type
  _CRYPT_CONTEXT_FUNCTIONS = record
    cFunctions: ULONG;
    rgpszFunctions: PPWSTR;
  end;
  CRYPT_CONTEXT_FUNCTIONS = _CRYPT_CONTEXT_FUNCTIONS;
  PCRYPT_CONTEXT_FUNCTIONS = ^_CRYPT_CONTEXT_FUNCTIONS;
  PPCRYPT_CONTEXT_FUNCTIONS = ^PCRYPT_CONTEXT_FUNCTIONS;

type
  _CRYPT_CONTEXT_FUNCTION_PROVIDERS = record
    cProviders: ULONG;
    rgpszProviders: PPWSTR;
  end;
  CRYPT_CONTEXT_FUNCTION_PROVIDERS = _CRYPT_CONTEXT_FUNCTION_PROVIDERS;
  PCRYPT_CONTEXT_FUNCTION_PROVIDERS = ^_CRYPT_CONTEXT_FUNCTION_PROVIDERS;
  PPCRYPT_CONTEXT_FUNCTION_PROVIDERS = ^PCRYPT_CONTEXT_FUNCTION_PROVIDERS;

//
// Provider Resolution Structures
//

type
  _CRYPT_PROPERTY_REF = record
    pszProperty: PWSTR;
    cbValue: ULONG;
    pbValue: PUCHAR;
  end;
  CRYPT_PROPERTY_REF = _CRYPT_PROPERTY_REF;
  PCRYPT_PROPERTY_REF = ^_CRYPT_PROPERTY_REF;
  PPCRYPT_PROPERTY_REF = ^PCRYPT_PROPERTY_REF;

type
  _CRYPT_IMAGE_REF = record
    pszImage: PWSTR;
    dwFlags: ULONG;
  end;
  CRYPT_IMAGE_REF = _CRYPT_IMAGE_REF;
  PCRYPT_IMAGE_REF = ^_CRYPT_IMAGE_REF;

type
  _CRYPT_PROVIDER_REF = record
    dwInterface: ULONG;
    pszFunction: PWSTR;
    pszProvider: PWSTR;
    cProperties: ULONG;
    rgpProperties: PPCRYPT_PROPERTY_REF;
    pUM: PCRYPT_IMAGE_REF;
    pKM: PCRYPT_IMAGE_REF;
  end;
  CRYPT_PROVIDER_REF = _CRYPT_PROVIDER_REF;
  PCRYPT_PROVIDER_REF = ^_CRYPT_PROVIDER_REF;
  PPCRYPT_PROVIDER_REF = ^PCRYPT_PROVIDER_REF;

type
  _CRYPT_PROVIDER_REFS = record
    cProviders: ULONG;
    rgpProviders: PPCRYPT_PROVIDER_REF;
  end;
  CRYPT_PROVIDER_REFS = _CRYPT_PROVIDER_REFS;
  PCRYPT_PROVIDER_REFS = ^_CRYPT_PROVIDER_REFS;
  PPCRYPT_PROVIDER_REFS = ^PCRYPT_PROVIDER_REFS;

//////////////////////////////////////////////////////////////////////////////
// CryptoConfig Functions ////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

function BCryptQueryProviderRegistration(
           pszProvider: LPCWSTR;
           dwMode: ULONG;
           dwInterface: ULONG;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_PROVIDER_REG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptEnumRegisteredProviders(
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_PROVIDERS): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Context Configuration Functions
//
function BCryptCreateContext(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           pConfig: PCRYPT_CONTEXT_CONFIG): NTSTATUS; stdcall; external 'bcrypt.dll'; // Optional

function BCryptDeleteContext(
           dwTable: ULONG;
           pszContext: LPCWSTR): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptEnumContexts(
           dwTable: ULONG;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_CONTEXTS): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptConfigureContext(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           pConfig: PCRYPT_CONTEXT_CONFIG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptQueryContextConfiguration(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_CONTEXT_CONFIG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptAddContextFunction(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           dwPosition: ULONG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptRemoveContextFunction(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptEnumContextFunctions(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_CONTEXT_FUNCTIONS): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptConfigureContextFunction(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           pConfig: PCRYPT_CONTEXT_FUNCTION_CONFIG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptQueryContextFunctionConfiguration(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_CONTEXT_FUNCTION_CONFIG): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptEnumContextFunctionProviders(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_CONTEXT_FUNCTION_PROVIDERS): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptSetContextFunctionProperty(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           pszProperty: LPCWSTR;
           cbValue: ULONG;
           pbValue: PUCHAR): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptQueryContextFunctionProperty(
           dwTable: ULONG;
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           pszProperty: LPCWSTR;
           pcbValue: PULONG;
           ppbValue: PPUCHAR): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Configuration Change Notification Functions
//

function BCryptRegisterConfigChangeNotify(
           phEvent: PHANDLE): NTSTATUS; stdcall; external 'bcrypt.dll';

function BCryptUnregisterConfigChangeNotify(
           hEvent: HANDLE): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Provider Resolution Functions
//
function BCryptResolveProviders(
           pszContext: LPCWSTR;
           dwInterface: ULONG;
           pszFunction: LPCWSTR;
           pszProvider: LPCWSTR;
           dwMode: ULONG;
           dwFlags: ULONG;
           pcbBuffer: PULONG;
           ppBuffer: PPCRYPT_PROVIDER_REFS): NTSTATUS; stdcall; external 'bcrypt.dll';

//
// Miscellaneous queries about the crypto environment
//
function BCryptGetFipsAlgorithmMode(
           pfEnabled: PByteBool): NTSTATUS; stdcall; external 'bcrypt.dll';

// BOOLEAN
// CngGetFipsAlgorithmMode(
// VOID
// );

implementation

end.